namespace GameName.UI.MemoryRoom.Space
{
    // 방을 드나드는 지점을 화면에 어떤 모양으로 보여줄지의 구분.
    //
    // 이 구분은 화면이 스스로 정하지 않는다 — 그래프에 사다리로 등록된
    // 연결인지(TryGetLadderLowerRoom) 아닌지를 물어 그대로 받아 적을 뿐이다.
    // 잠김 여부나 이동 가능 여부도 여기 담지 않는다: 그건 이동 처리기가
    // 판정할 몫이고, 이 값은 "세로로 그릴 것인가 옆으로 그릴 것인가"만 정한다.
    public enum RoomExitKind
    {
        // 좌우 벽에 난 문. 같은 층의 다른 공간(다른 방, 계단, 분석실 등)으로
        // 이어진다.
        Door,

        // 위아래로 이어진 사다리.
        Ladder
    }
}
